apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-clubs
  namespace: projet51
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-clubs
  template:
    metadata:
      labels:
        app: api-clubs
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: api-clubs
        image: guigui2515/projet51:back-api-clubs
        ports:
          - containerPort: 4001
        env:
          - name: PORT
            value: "4001"
          - name: MONGO_URI
            value: "mongodb://admin:password123@mongo.projet51.svc.cluster.local:27017/projet51_db?authSource=admin"
